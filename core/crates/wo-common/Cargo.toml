[package]
name = "wo-common"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Shared types, errors, and test harness for the World-Office document editing suite"
repository = "https://codeberg.org/World-Office/server"
homepage = "https://codeberg.org/World-Office/server"
keywords = ["document", "office", "word-processor", "common"]
categories = ["parser-implementations"]

[dependencies]
anyhow = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

