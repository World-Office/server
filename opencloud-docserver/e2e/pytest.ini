[pytest]
markers =
    gui: browser-level tests against the cloud GUI (OpenCloud web + editors)
    wopi: protocol-level tests against the docserver (no browser)
addopts = -q --strict-markers
filterwarnings =
    ignore::urllib3.exceptions.InsecureRequestWarning
